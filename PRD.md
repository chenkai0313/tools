# PRD: 站长工具平台 (Webmaster Tools)

## 1. 项目概述

### 1.1 项目名称
站长工具平台（Webmaster Tools）

### 1.2 项目定位
纯前端、中英双语的站长工具门户网站。所有工具在浏览器端完成计算，无后端依赖，可部署到 Vercel 等静态托管平台。

### 1.3 目标用户
站长、开发者、运维人员及对网络工具有需求的普通用户。

### 1.4 设计参考
视觉风格参考 [jyshare.com](https://www.jyshare.com/)——信息门户型工具站：顶部固着导航栏，内容按分类分区排列，功能优先，装饰克制，强调可扫读性。

---

## 2. 技术栈

| 层 | 技术 | 说明 |
|---|------|------|
| 框架 | Next.js 16.2.4 | React 19 |
| 语言 | TypeScript 5 | strict 模式 |
| 样式 | Tailwind CSS v4 | 原子化 CSS |
| 国际化 | next-intl 或 i18n 方案 | 中英双语 |
| 部署 | Vercel / 静态导出 | 纯前端 |
| 规范 | ESLint + 简洁约定 | 不引入 commitlint 等 |

---

## 3. 网站结构

```
[Logo]  [首页]  [文章]  [工具▼]
                       ├── 时间转换
                       ├── JSON 工具
                       ├── Base64 图片
                       ├── 密码生成
                       └── Cron 表达式
```

### 3.1 导航设计
- **一级菜单**：首页、文章、工具
- **工具菜单**：含二级下拉菜单，列出所有工具项
- 无登录/注册入口

### 3.2 页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | 首页 | 展示热点文章、文章分类、各分类文章列表 |
| `/articles` | 文章列表 | 全部分类 + 文章列表 |
| `/articles/[slug]` | 文章详情 | Markdown 渲染 |
| `/tools/time` | 时间转换工具 | |
| `/tools/json` | JSON 工具 | |
| `/tools/base64` | Base64 图片工具 | |
| `/tools/password` | 密码生成器 | |
| `/tools/cron` | Cron 表达式工具 | |

---

## 4. 首页设计

### 4.1 布局结构
```
┌─────────────────────────────────────┐
│  Logo   首页   文章   工具 ▼         │ ← 顶部导航栏
├─────────────────────────────────────┤
│                                     │
│  ┌─────────── 热点文章 ───────────┐ │
│  │  📰 文章标题 1        →       │ │
│  │  📰 文章标题 2        →       │ │
│  │  📰 文章标题 3        →       │ │
│  └────────────────────────────────┘ │
│                                     │
│  ┌─── 分类 1 ─── ┌─── 分类 2 ───┐ │
│  │  ▸ 文章 1     │  ▸ 文章 4    │ │
│  │  ▸ 文章 2     │  ▸ 文章 5    │ │
│  │  ▸ 文章 3     │  ▸ 文章 6    │ │
│  └────────────── └──────────────┘ │
│                                     │
│  ┌─── 分类 3 ─── ┌─── 分类 4 ───┐ │
│  │  ...          │  ...          │ │
│  └────────────── └──────────────┘ │
│                                     │
│  ┌────── 底部信息 ────────────────┐ │
│  │  Copyright 等信息              │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 4.2 区块说明
1. **热点文章**：轮播或列表展示，标记为"HOT"标签
2. **文章分类**：网格布局，每个分类下展示最近 3-4 篇文章
3. **底部**：版权信息、友情链接等

---

## 5. 工具功能详情

### 5.1 时间转换工具
**路由**: `/tools/time`

| 功能 | 说明 |
|------|------|
| 时间戳→日期 | 输入秒/毫秒时间戳，输出可读日期字符串 |
| 日期→时间戳 | 选择或输入日期时间，输出秒/毫秒时间戳 |
| 时区支持 | 显示 UTC 和本地时间，支持选择时区 |
| 格式预设 | 常见格式快速切换（YYYY-MM-DD、RFC3339 等） |
| 实时预览 | 输入即转换，无按钮提交 |

### 5.2 JSON 工具
**路由**: `/tools/json`

| 功能 | 说明 |
|------|------|
| JSON 格式化 | 美化/压缩 JSON，带语法高亮 |
| 校验 | 检测 JSON 语法错误并定位 |
| 字段提取 | 输入 JSON Path 或直接选择字段，提取特定值 |
| 生成结构体 | JSON → 类型定义，支持 5 种语言 |
| | - **Go**: `struct` 标签 |
| | - **TypeScript**: `interface` / `type` |
| | - **Rust**: `struct + serde` |
| | - **Python**: `dataclass` |
| | - **Java**: `class` |

### 5.3 Base64 图片工具
**路由**: `/tools/base64`

| 功能 | 说明 |
|------|------|
| Base64→图片 | 输入 Base64 字符串，实时渲染图片预览 |
| 图片→Base64 | 拖拽/选择图片，输出 Base64 字符串 |
| 格式检测 | 自动识别 MIME 类型（png/jpg/gif/webp/svg） |
| 复制结果 | 一键复制 Base64 结果 |
| 拖拽上传 | 支持拖拽上传图片文件 |

### 5.4 密码生成器
**路由**: `/tools/password`

| 功能 | 说明 |
|------|------|
| 字符类型开关 | 大写字母、小写字母、数字、特殊符号 |
| 长度控制 | 滑块或数字输入，4-128 位 |
| 排除相似字符 | 可选排除 `1lI` `0O` 等易混淆字符 |
| 自定义字符集 | 可选包含/排除特定字符 |
| 密码数量 | 一次生成多条，供选择 |
| 强度指示器 | 实时显示密码强度 |
| 一键复制 | 点击复制到剪贴板 |

### 5.5 Cron 表达式工具
**路由**: `/tools/cron`

| 功能 | 说明 |
|------|------|
| Cron→可读文本 | 输入标准 5 位/6 位 Cron 表达式，翻译为自然语言 |
| | 如 `*/5 * * * *` → "每 5 分钟执行一次" |
| 自然语言→Cron | 输入时间描述，生成对应的 Cron 表达式 |
| | 如 "每天凌晨 2 点" → `0 2 * * *` |
| | 如 "每周一至周五上午 9 点" → `0 9 * * 1-5` |
| 常用预设 | 快速选择常见周期（每分钟、每小时、每天等） |
| 最近执行时间 | 显示接下来 5 次执行时间 |

---

## 6. 国际化

### 6.1 策略
- 默认根据浏览器语言 `Accept-Language` 切换
- 页面右上角提供手动切换开关（中文 / English）
- 使用 next-intl 或等效 i18n 方案

### 6.2 范围
- 导航、页脚等 UI 元素 → 全量翻译
- 工具界面 → 全量翻译
- 文章内容 → 以中文为主，英文版后续补充

---

## 7. 目录结构约定

```
tools/
├── app/
│   ├── page.tsx              # 首页
│   ├── layout.tsx            # 根布局（导航 + 页脚）
│   ├── globals.css           # 全局样式
│   ├── articles/
│   │   ├── page.tsx          # 文章列表
│   │   └── [slug]/page.tsx   # 文章详情
│   └── tools/
│       ├── time/page.tsx     # 时间转换
│       ├── json/page.tsx     # JSON 工具
│       ├── base64/page.tsx   # Base64 图片
│       ├── password/page.tsx # 密码生成
│       └── cron/page.tsx     # Cron 表达式
├── components/
│   ├── Header.tsx            # 顶部导航
│   ├── Footer.tsx            # 页脚
│   ├── ArticleCard.tsx       # 文章卡片
│   └── ...                   # 其他通用组件
├── lib/
│   ├── utils.ts              # 通用工具函数
│   ├── time.ts               # 时间转换逻辑
│   ├── json.ts               # JSON 处理逻辑
│   ├── base64.ts             # Base64 处理逻辑
│   ├── password.ts           # 密码生成逻辑
│   └── cron.ts               # Cron 解析逻辑
├── data/
│   └── articles/             # 固定文章 Markdown 文件
├── i18n/
│   ├── zh.ts                 # 中文翻译
│   └── en.ts                 # 英文翻译
├── public/
│   └── images/               # 图片资源
└── package.json
```

### 7.1 命名约定
- **组件**: PascalCase（如 `ArticleCard.tsx`）
- **工具函数**: camelCase（如 `formatTimestamp.ts`）
- **路由文件**: Next.js App Router 约定
- **样式**: Tailwind CSS，不写自定义 CSS 文件
- **一个文件只导出一个默认组件**

---

## 8. 部署

- 构建命令: `npm run build`
- 导出方式: Next.js 静态导出 (`output: 'export'`) 或 Vercel 直接部署
- 平台: Vercel（推荐）

---

## 9. 迭代计划

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| **Phase 1** | 项目脚手架 + 导航框架 + 首页 + 文章页面 | P0 |
| **Phase 1** | 时间转换工具 + JSON 工具 | P0 |
| **Phase 2** | Base64 图片工具 + 密码生成器 | P1 |
| **Phase 2** | Cron 表达式工具 | P1 |
| **Phase 3** | 国际化（中英双语） | P1 |
| **Phase 4** | 文章内容填充 + 更多工具扩展 | P2 |

---

## 10. SEO 要求

每个页面必须设置独立的 SEO 元信息，利用 Next.js Metadata API：

| 字段 | 说明 |
|------|------|
| `title` | 页面标题，格式：`具体标题 - 站长工具` |
| `description` | 页面描述，120 字以内，概括页面内容 |
| `keywords` | 页面关键词，与工具/文章内容相关 |
| `openGraph` | OG 协议，包含标题、描述、图片 |
| `alternates` | 中英文页面 Link 标签，告知搜索引擎双语版本 |

### 实现方式

```typescript
// 每个 page.tsx 导出 metadata
export const metadata: Metadata = {
  title: '时间戳转换 - 站长工具',
  description: '在线时间戳与日期时间互转工具，支持秒级和毫秒级时间戳，...',
  keywords: '时间戳, timestamp, 时间转换, Unix时间戳',
  alternates: {
    languages: {
      'zh': '/tools/time',
      'en': '/en/tools/time',
    },
  },
}
```

工具页面还需注入 JSON-LD 结构化数据，提升搜索引擎对工具功能的理解。

---

## 11. 非功能性需求

- 所有工具纯前端执行，不发送网络请求
- 响应式设计，适配移动端
- 浅色/深色主题支持（可选）
- 首屏加载性能优先，工具逻辑懒加载
